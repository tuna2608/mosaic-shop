import styled from "styled-components";
import { Populate, PopulateContainer } from "../MostPopularComponent/style";


export const PopulateCenter = styled(Populate)`
    justify-content: center;
`

export const PopulateCenterContainer = styled(PopulateContainer)`
    padding: 0 120px;

`
